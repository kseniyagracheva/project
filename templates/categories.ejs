<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Categories</title>
</head>
<body>
    <h1>Categories</h1>
    <a href="/categories/add">Add New Category</a>
    <ul>
        <%categories.forEach( category => { %>
        <li>
            <%= category.name %>
            <a href="/categories/edit/<%= category._id %>">Edit</a>
            <a href="/categories/delete/<%= category._id %>" onclick="return confirm('Are you sure?');">Delete</a>
        </li>
        <% }) %>
    </ul>
    <a href="/">Back to Home</a>
</body>
</html>